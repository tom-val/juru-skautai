{
  "name": "juru-skautai-backend",
  "version": "1.0.0",
  "description": "Lambda functions for the Jūrų skautai abilities tracker (members API, Cognito authorizer, pre-signup trigger)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "aws-jwt-verify": "^5.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "typescript": "~5.7.0"
  }
}
