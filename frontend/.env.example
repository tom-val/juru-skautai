# Backend wiring for the abilities tracker (auth + persistence).
# In CI these are injected from Terraform outputs (see .github/workflows/deploy-prod.yml).
# For local dev, copy to .env.local and point at a deployed stack to exercise login/data.
VITE_API_URL=
VITE_USER_POOL_ID=
VITE_USER_POOL_CLIENT_ID=
